"""
Main entry point for the Package Sorting API.
"""

import uvicorn
from .api import app

if __name__ == "__main__":
    uvicorn.run(
        "src.api:app",
        host="0.0.0.0",
        port=8000,
        reload=True,
        log_level="info"
    )